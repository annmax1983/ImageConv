# ImageConv

[English](../README.md) | [中文](README_zh.md) | [Español](README_es.md) | [Deutsch](README_de.md) | 日本語 | [Français](README_fr.md)

PNG・JPG・WEBP・AVIF形式を相互変換できる軽量ブラウザ拡張機能です。変換はすべてローカルで完結し、データが外部に送信されることはありません。

> Chromium系ブラウザ対応 · Manifest V3 · トラッキングなし · すべてブラウザ内で処理

---

## ImageConvの特長

ほとんどの画像変換ツールは、ファイルをリモートサーバーにアップロードする必要があります。ImageConvはCanvas APIを使用してブラウザ内ですべて完結するため、データがマシンの外に出ることはありません。

| 特長 | 詳細 |
|-----------|--------|
| 🔒 **プライバシー最優先** | すべての処理はブラウザのメモリ内で実行されます。サーバーへのアップロードもトラッキングもありません。 |
| ⚡ **高速変換** | 5MB以下の画像は500ms以内に変換完了。サーバーとの往復待ちが不要です。 |
| 🎯 **依存なし** | バニラJavaScriptとネイティブChrome APIのみで構築。フレームワーク不使用、無駄なコードなし。 |
| 🌍 **6言語対応** | ブラウザの言語設定を自動検出します（英語・スペイン語・ドイツ語・日本語・フランス語・中国語）。 |
| 📋 **コピー＆保存** | 画像を右クリックしてファイルとして保存するか、クリップボードに直接コピーできます。 |
| 📁 **ドラッグ＆ドロップ** | ローカル画像をポップアップにドロップしてすばやく変換できます。 |

---

## 機能一覧

### 無料機能

| 機能 | 説明 |
|---------|-------------|
| 🖼️ **右クリック変換** | Webページ上の任意の画像をコンテキストメニューからPNG・JPG・WEBP・AVIFに変換 |
| 📱 **HEIC/HEIF対応** | iPhone写真をそのまま変換 — ドラッグ＆ドロップ・貼り付け・右クリックに対応。内蔵WASMデコーダでローカル処理 |
| 📐 **サイズ変更** | 倍率（%）またはピクセル指定でリサイズ。片方を空欄にすると比率を維持。ポップアップと右クリック変換の両方に適用 |
| 📋 **クリップボードコピー** | 右クリック → PNG/JPG/WEBP/AVIFとしてコピー — メール・チャット・ドキュメントに貼り付け可能 |
| 📊 **ファイルサイズ比較** | トースト通知で元のサイズと変換後のサイズ、削減率を表示（実際のファイルサイズに基づく） |
| 🎚️ **品質調整** | ポップアップ内のスライダーでJPG・WEBP・AVIFの出力品質を細かく調整 |
| 🔗 **スマートリンククリーニング** | CDN追跡パラメータを除去し、オリジナル画像を取得 |
| 🌐 **クロスオリジン対応** | Service Worker経由で任意のWebサイトから画像を取得 |
| 🔤 **6言語i18n** | コンテキストメニューとUIがブラウザの言語設定に自動対応 |
| 🛡️ **JPG白色填充** | PNG→JPG変換時、透明背景を自動的に白色で填充 |
| ⏱️ **重複ガード** | 同じ画像に対する2秒以内の連続クリックを無視 |

### Premium機能（ライセンス必要）

| 機能 | 説明 |
|---------|-------------|
| ⭐ **ドラッグ＆ドロップ変換** | ローカル画像をポップアップにドロップまたは貼り付け（Ctrl+V）してすばやく変換 |
| 📁 **一括ローカル変換** | 複数のローカルファイルを一度に変換 |

---

## 無料版 vs Premium

| | 無料版 | Premium |
|---|:---:|:---:|
| 右クリック画像変換 | ✅ | ✅ |
| クリップボードコピー | ✅ | ✅ |
| 品質調整 | ✅ | ✅ |
| HEIC/HEIF入力 | ✅ | ✅ |
| サイズ変更 | ✅ | ✅ |
| クロスオリジン画像取得 | ✅ | ✅ |
| ドラッグ＆ドロップローカル変換 | — | ✅ |
| 一括ローカルファイル変換 | — | ✅ |

---

## プレビュー

<!-- 実際のスクリーンショットに差し替えてください -->
<p align="center">
  <img src="screenshots/popup.png" alt="ImageConv Popup" width="360">
</p>

<p align="center">
  <img src="screenshots/context-menu.png" alt="ImageConv Context Menu">
</p>

---

## 対応ブラウザ

| ブラウザ | ステータス |
|---------|--------|
| Google Chrome | ✅ 完全対応 |
| Microsoft Edge | ✅ 完全対応 |
| Brave | ✅ 対応 |
| Opera | ✅ 対応 |
| Vivaldi | ✅ 対応 |
| Chromium系ブラウザ全般 | ✅ 対応（Manifest V3） |

---

## インストール

### ソースから（開発者モード）

1. ブラウザの拡張機能ページを開きます：
   - **Chrome**: `chrome://extensions/`
   - **Edge**: `edge://extensions/`
2. **デベロッパーモード**を有効にします（右上のトグル）
3. **パッケージ化されていない拡張機能を読み込む**をクリックし、`pic-convert`フォルダを選択します
4. ツールバーに✨ ImageConvアイコンが表示されます

---

## 使い方

### 右クリック変換

1. Webページ上の任意の画像を右クリックします
2. コンテキストメニューから**ImageConv**を選択します
3. **名前を付けて保存**または**コピー**を選択します
4. フォーマットを選択：PNG・JPG・WEBP・AVIF
5. 完了 — ファイルが即座にダウンロードされるか、画像がクリップボードにコピーされます

### ドラッグ＆ドロップ（Premium）

> ⚠️ ドラッグ＆ドロップはPremium機能です。ライセンスキーが必要です。

1. ツールバーのImageConvアイコンをクリックしてポップアップを開きます
2. 右上の🔑ボタンをクリックしてライセンスキーを入力します
3. 有効化後、ローカル画像ファイルをドロップゾーンにドラッグします
4. 変換先フォーマットを選択します
5. 変換されたファイルが自動的にダウンロードされます

ライセンスキーは[annmax1983.com](https://www.annmax1983.com/checkout.html?plugin=imageconv)で購入できます。

### 品質設定

ポップアップを開いて、スライダーでJPG・WEBP・AVIFの出力品質を調整できます。PNGは常にロスレスです。設定は自動的に保存されます。

### サイズ変更

ポップアップの「サイズ変更」セクションで、倍率（5%〜200%）またはピクセル指定のリサイズが可能です。片方を空欄にすると自動的に比率を維持します。この設定はドラッグ＆ドロップ変換と右クリック変換の両方に適用され、デフォルトは元のサイズです。

### HEIC/HEIF（iPhone写真）

`.heic` / `.heif` ファイルをポップアップにドロップするか（Webページ上のHEIC画像リンクも変換可能）、PNG/JPG/WEBPとして保存できます。デコードはWASMにより完全にローカルで行われ、アップロードは一切ありません。

---

## コンテキストメニュー構成

```
ImageConv
├── PNGとして保存（ロスレス）
├── JPGとして保存（高品質）
├── WEBPとして保存（コンパクト）
├── AVIFとして保存（最高圧縮）
├── PNGとしてコピー（ロスレス）
├── JPGとしてコピー（高品質）
├── WEBPとしてコピー（コンパクト）
└── AVIFとしてコピー（最高圧縮）
```

**コピー**メニューと**保存**メニューは、画像または画像ファイルへのリンク（`.png`、`.jpg`、`.jpeg`、`.webp`、`.avif`、`.gif`、`.bmp`、`.jfif`、`.svg`）を右クリックした場合にのみ表示されます。

---

## プライバシー

ImageConvはプライバシーを核となる原則として構築されています：

- ✅ **データアップロードなし** — 画像処理はすべてブラウザのメモリ内で実行されます
- ✅ **アナリティクスなし** — トラッキング、テレメトリ、リモート呼び出しなし
- ✅ **Cookie不使用** — ブラウザCookieの読み書きは行いません
- ✅ **閲覧履歴不参照** — ブラウザデータへのアクセスは行いません
- ✅ **一時メモリのみ使用** — 画像は変換中にメモリに存在し、変換後すぐに破棄されます
- ✅ **最小限の権限** — 必要最小限の権限のみを要求：`contextMenus`、`downloads`、`offscreen`、`storage`、`clipboardWrite`

---

## 動作の仕組み

```
画像を右クリック
       ↓
Service Workerが画像blobを取得（クロスオリジン対応）
       ↓
Offscreen Document（Canvasアクセス可能な非表示DOM）にblobを送信
       ↓
Canvasがネイティブ解像度で画像を描画
       ↓
指定品質でターゲットフォーマットとしてエクスポート
       ↓
data URLを返却 → ダウンロードまたはクリップボードコピーを実行
       ↓
すべてのリソース（blob、canvas、画像要素）を破棄
```

> **Offscreenが必要な理由** ChromeのManifest V3ではService Workerとしてバックグラウンドが実行され、DOMアクセスがありません。Canvas APIにはDOMが必要なため、ChromeのOffscreen APIを使用して画像処理用の非表示ドキュメントを作成しています。

---

## エクスポート品質のデフォルト値

| フォーマット | 品質 | 備考 |
|--------|---------|-------|
| PNG | ロスレス | 品質設定なし — 常に最高品質と透明度を保持 |
| JPG | 92 | 高品質、サイズと鮮明さのバランスに優れる |
| WEBP | 90 | モダンフォーマット、同等品質でJPGより約25〜35%小さい |
| AVIF | 70 | 次世代フォーマット、WEBPより約20〜30%小さい、Web配信に最適 |

すべての品質値はポップアップ内のスライダーで調整可能です（範囲：10〜100）。

> ℹ️ AVIFは内蔵WASMエンコーダによって出力されます（ChromiumのCanvasはネイティブにAVIFエンコードに対応していません）。超大きな画像（2500万ピクセル超）は自動的にWEBPにフォールバックし、通知が表示されます。

---

## 著作権に関する免責事項

この拡張機能は、ユーザーの個人的なオフライン処理のためのローカル画像フォーマット変換機能のみを提供します。Webページ上のすべての画像、写真、グラフィックリソースは、オリジナルの著作権者に帰属します。ユーザーは変換した画像を商業複製、無断配布、二次創作その他の著作権侵害行為に使用してはなりません。不適切な使用により生じたすべての法的責任は、ユーザーが単独で負うものとします。

## クロスオリジン画像に関する注意

クロスオリジン画像取得機能は、ローカルフォーマット変換のための画像リソース取得にのみ使用されます。この機能を使用してWebサイトの画像リソースを大量にクロールすることは禁止されており、Webサイトのアクセサリー規則に違反する可能性があります。

---

## ソースコードに関する注意

> ⚠️ **このリポジトリにはソースコードは公開されていません。** 使用ドキュメント、リリースノート、サポートリソースのみを含みます。拡張機能はChrome Web Storeを通じてのみ配布されます。オフラインインストールパッケージやエンドユーザー向けソースコードは提供されません。

---

## ライセンス

Copyright © 2026 ImageConv. All rights reserved.

### ライセンスの仕組み

ImageConvはデバイスベースのライセンスシステムを採用しています：

1. [annmax1983.com](https://www.annmax1983.com/checkout.html?plugin=imageconv)でライセンスキーを**購入**
2. ポップアップの🔑ボタンをクリックしてキーを入力し、**有効化**
3. キーはデバイス（ハードウェアフィンガープリント）に紐付けられます — 1キー＝1デバイス
4. ライセンスは24時間ごとにオンラインで検証され、オフラインでは最大7日間動作します

### 無料版と有料版の違い

| 機能 | 無料版 | Premium |
|---------|:----:|:-------:|
| 右クリック画像変換（Web画像） | ✅ | ✅ |
| PNG / JPG / WEBP / AVIFとして保存 | ✅ | ✅ |
| クリップボードコピー | ✅ | ✅ |
| 品質調整スライダー | ✅ | ✅ |
| HEIC/HEIF入力（WASMデコード） | ✅ | ✅ |
| サイズ変更（倍率 / ピクセル指定） | ✅ | ✅ |
| クロスオリジン画像取得 | ✅ | ✅ |
| スマートリンククリーニング | ✅ | ✅ |
| ドラッグ＆ドロップローカルファイル変換 | ❌ | ✅ |
| 一括ローカルファイル変換 | ❌ | ✅ |

**まとめ**：すべてのWeb画像機能（右クリック保存・コピー）は**無料**です。ローカルファイル変換（ドラッグ＆ドロップ）には**Premiumライセンス**が必要です。

---

## ❤️ サポート

ImageConvが役に立った場合は、プロジェクトの支援をご検討ください！

**[👉 Ko-fiで支援する](https://ko-fi.com/annmax?ref=imageconv)**

**[🌐 公式サイト](https://www.annmax1983.com/extensions/imageconv)**
